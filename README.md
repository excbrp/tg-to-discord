# tg-to-discord

rename `config.yml-sample` to `config.yml`.

replace the contents inside `config.yml` to your own values.

install requirements
```pip install -r requirements.txt```

run the bot
`python forward.py config.yml`
